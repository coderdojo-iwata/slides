{
  "name": "slides",
  "version": "1.0.0",
  "author": "CoderDojoIwata <iwata.jp@coderdojo.com>",
  "license": "WTFPL",
  "private": true,
  "scripts": {
    "build": "rimraf public && npm run -s deck",
    "deck": "marp --no-stdin slides/* && mkdir -p public && find ./slides -name '*.html' -exec mv {} public/ \\; && ncp assets public/assets",
    "start": "marp -ps ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0",
    "ncp": "^2.0.0",
    "rimraf": "^6.0.1"
  }
}
