{
  "name": "slides",
  "version": "1.0.0",
  "author": "CoderDojoIwata <iwata.jp@coderdojo.com>",
  "license": "WTFPL",
  "private": true,
  "scripts": {
    "build": "rimraf docs && npm run -s deck && npm run -s home",
    "deck": "marp --no-stdin slides/* && mkdir -p docs && find ./slides -name '*.html' -exec mv {} docs/ \\; && ncp slides/assets docs/assets",
    "home": "node scripts/generate-home.js",
    "start": "marp -ps ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0",
    "ncp": "^2.0.0",
    "rimraf": "^6.0.1"
  }
}
