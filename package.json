{
  "name": "slides",
  "version": "1.0.0",
  "author": "CoderDojoIwata <iwata.jp@coderdojo.com>",
  "license": "WTFPL",
  "private": true,
  "scripts": {
    "build": "rimraf public && npm run -s deck && npm run -s top && npm run -s open",
    "plantuml": "node scripts/generate-plantuml-images.js",
    "top": "node scripts/generate-top.js",
    "deck": "marp --no-stdin slides/* && npm run -s plantuml && mkdir -p public && find ./slides -name '*.html' -exec mv {} public/ \\; && ncp slides/assets public/assets",
    "start": "marp -ps .",
    "open": "open public/index.html"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0",
    "ncp": "^2.0.0",
    "node-plantuml": "^0.9.0",
    "rimraf": "^6.0.1"
  }
}
